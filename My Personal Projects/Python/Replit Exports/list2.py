input =input( "enter a word: ") #input
list = []  #creating empty list
for i in input:   #for each letter in input, we are having the letter be individually added to the list 
  #"MOM" = kl["m","o","m"]
  list.append(input)
  print (list)

while True:
  for j in range(list[]):
    if list[j] == list[x-j]:
      j +=1
      
    


'''
def word_to_list(input):

    kl = []
 
    for kl in input:
        letters.append(input)
    # Return the list of individual letters
    return letters
  User
    input =input( "hello")
    letters = word_to_list(input)
    print(letters)
'''

    