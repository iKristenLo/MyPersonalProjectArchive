company = company_name
#create file with company software
company.footprint(value,effort,risk)
profit = value - (effort * risk)
if profit < 0:
  company.enumerate()
  select attacktype()
  #make class for risk value effort attack_type select Dos engage_botnet fword launchddos
  if select == DoS:
    engage_botnet(fword)
    fword.launchDDos(company)
  if company == access:
    company.gainAccess(account)
    account.Elevate()
    company.installBackdoor(account)
else:
  Break()